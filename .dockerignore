node_modules
.next
.git
.gitignore
.DS_Store
out
build
Dockerfile
docker-compose.yml
*.md

# Never bake local secrets into the image — Next's standalone output
# copies .env files, so they must be excluded at the build context.
.env
.env.*
!.env.example
